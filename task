#!/bin/bash

export USER_UID=$(whoami | id -u)
export USER_GID=$(whoami | id -g)

if [ "$1" = "up" ]; then
    docker-compose up ${@:2}
elif [ "$1" = "down" ]; then
    docker-compose down --rmi local
fi
